spec_version: "0.4.0"
name: email-agent
version: 1.0.0
description: An agent that summarizes unread emails by urgency

model:
  preferred: "anthropic:claude-sonnet-4-6"
  fallback: []

tools:
  - read
  - write

runtime:
  max_turns: 15
  timeout: 120
