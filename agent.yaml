spec_version: "0.4.0"
name: standup-agent
version: 1.0.0
description: Writes a daily standup digest from git activity

model:
  preferred: "anthropic:claude-sonnet-4-6"
  fallback: []

tools:
  - read

runtime:
  max_turns: 3
  timeout: 60
