spec_version: "0.4.0"
name: refactor-agent
version: 1.0.0
description: An agent that refactors codebases

model:
  preferred: "anthropic:claude-sonnet-4-6"
  fallback: []

tools:
  - cli
  - read
  - write

runtime:
  max_turns: 50
  timeout: 120
